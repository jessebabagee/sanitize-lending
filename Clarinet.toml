[project]
name = "sanitize-lending"
authors = ["SanitizeDev Team"]
description = "A decentralized lending platform with robust safety and reputation mechanisms"
telemetry = false
requirements = []
[contracts.lending-core]
path = "contracts/lending-core.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
